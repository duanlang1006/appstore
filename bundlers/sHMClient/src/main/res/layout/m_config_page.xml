<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@color/content_bg_color"
    android:orientation="vertical" >

    <TextView
        style="@style/QuickView.Title"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:text="@string/config_market" />

    <ScrollView
        android:layout_width="fill_parent"
        android:layout_height="0px"
        android:layout_weight="1" >

        <LinearLayout
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical" >

            <!-- 软件图标 -->

            <TextView
                style="@style/ConfigView.Band"
                android:layout_width="fill_parent"
                android:layout_height="wrap_content"
                android:text="@string/config_ref_image" />

            <include
                android:id="@+id/appIconConfigView"
                layout="@layout/config_check" />

            <include
                android:id="@+id/appScreenshotConfigView"
                layout="@layout/config_check" />

            <include
                android:id="@+id/checkSoftwareUpdateConfigView"
                layout="@layout/config_check" />
        </LinearLayout>
    </ScrollView>

</LinearLayout>